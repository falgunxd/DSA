#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> sax;
    for (int i = 0; i < 4; i++)
    {
        int n;
        cin >> n;
        sax.push_back(n);
    }
    for (iterator it = sax.begin(); it != sax.end(); ++it)
    // for (int i=0; i<sax.size(); i++)
    {
        cout << sax[i];
    }
    for (iterator )

    return 0;
}